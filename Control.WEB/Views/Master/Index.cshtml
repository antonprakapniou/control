@model IEnumerable<IdentityMasterVM>
<br />
<table class="table table-responsive table-dark table-hover table-sm align-middle" style="width:100%">
    <thead>
        <tr>
            <th class="text-start align-middle">
                <div class="col-12">
                    <a asp-controller="Master" asp-action="Create" class="btn btn-dark">
                        <i class="bi bi-plus-lg"></i>&nbsp;Master
                    </a>
                </div>
            </th>
            <th class="text-center align-middle">Phone</th>
            <th class="text-center align-middle">Email</th>
            <th class="text-center align-middle" style="width:8% "><i class="bi bi-gear-wide-connected"></i></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count()>0)
        {
            @foreach (var item in Model)
            {
                <tr>
                    <td class ="text-start align-middle">@item.UserName</td>
                    <td class="text-center align-middle">@item.Email</td>
                    <td class="text-center align-middle">@item.PhoneNumber</td>                    
                    <td class="text-center align-middle">
                        <div class="btn-toolbar justify-content-between" role="toolbar">
                            <a asp-controller="Master" asp-action="Update" asp-route-id="@item.Id" class="btn btn-outline-warning">
                                <i class="bi bi-pencil-fill"></i>
                            </a>
                            <form method="post" asp-controller="Master" asp-action="Delete" asp-route-id="@item.Id">
                                <button type="submit" class="btn btn-outline-danger">
                                    <i class="bi bi-trash3"></i>
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
            }
        }

        else
        {
            <tr>
                <td colspan="4">Mater list is empty</td>
            </tr>
        }
    </tbody>
</table>