@model IEnumerable<OwnerVM>
<br />
<table class="table table-responsive table-dark table-hover align-middle" style="width:100%">
    <thead>
        <tr>
            <th style="width:18%">
                <div class="col-12 text-start">
                    <a asp-controller="Owner" asp-action="Create" class="btn btn-dark">
                        <i class="bi bi-plus-lg"></i>&nbsp;Shop
                    </a>
                </div>
            </th>
            <th class="text-center align-middle" style="width:18% ">Production</th>
            <th class="text-center align-middle" style="width:18% ">Master</th>
            <th class="text-center align-middle" style="width:18% ">Phone</th>
            <th class="text-center align-middle" style="width:18% ">Email</th>
            <th class="text-center align-middle" style="width:10% "><i class="bi bi-gear-wide-connected"></i></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count()>0)
        {
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Shop</td>
                    <td class="text-center">@item.Production</td>
                    <td class="text-center">@item.Master</td>
                    <td class="text-center">@item.Phone</td>
                    <td class="text-center">@item.Email</td>
                    <td class="text-center">
                        <div class="btn-group" role="group" aria-label="Basic mixed styles example">
                            <a asp-controller="Owner" asp-action="Update" asp-route-id="@item.Id" class="btn btn-outline-warning">
                                <i class="bi bi-pencil-fill"></i>
                            </a>
                            <form method="post" asp-controller="Owner" asp-action="Delete" asp-route-id="@item.Id">
                                <button type="submit" class="btn btn-outline-danger">
                                    <i class="bi bi-trash3"></i>
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
            }
        }

        else
        {
            <tr>
                <td colspan="6">Nomination list is empty</td>
            </tr>
        }
    </tbody>
</table>